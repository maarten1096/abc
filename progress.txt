
- Implemented a complete UI overhaul to create a ChatGPT-style layout.
- Refactored the `Sidebar`, `ProfileMenu`, and `MainArea` components to create the new layout.
- Replaced placeholder icons with proper icons from the `react-icons` library.
- Removed the login requirement for now.
- The next step will be to re-implement the user authentication and profile management features within the new design.
- Re-implemented user authentication and profile management features.
- Updated the `ProfileMenu` component to handle user sessions and login/logout functionality.
- Created a new login page with a Supabase UI Auth component.
- The next step will be to merge the local data with the remote data after the user logs in.
- Implemented local data merging for the `SummaryTool`.
- Anonymous users can now generate summaries, which are stored in `localStorage`.
- When a user logs in, any locally stored summaries are automatically saved to their account.
- Applied the same local data merging logic to the `FlashcardTool` and `QuizTool`.
- Anonymous users can now generate flashcards and quizzes and save them to their account after logging in.
